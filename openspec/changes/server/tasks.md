# Tasks: Self-hosted server — Milestone 4 (v0.3)

## Review Workload Forecast

| Field | Value |
|-------|-------|
| Estimated changed lines | ~7,000–9,500 (authored additions+deletions; sqlc-generated store code and `docs/openapi.yaml` prose excluded from this authored count, both still included in complete diff/snapshot identity) |
| 400-line budget risk | High |
| Chained PRs recommended | Yes |
| Suggested split | 9 work units, see below |
| Delivery strategy | ask-on-risk |
| Chain strategy | pending |

Decision needed before apply: Yes
Chained PRs recommended: Yes
Chain strategy: pending
400-line budget risk: High

This milestone is materially larger than Milestone 2 (~2800–3200) or Milestone 3 (~2600–3400): it adds five entirely new packages (`server`, `api`, `auth`, `store`+`sqlitestore`+`storetest`+migrations, `sync`), a new client source, a credentials subsystem, four new CLI commands, an OpenAPI contract with a coverage test, and CI/release changes — none of which existed before. Repeating the M2/M3 single-branch precedent is not recommended here; the natural slicing (store → auth → api → sync → ServerSource) is largely sequential, which favors a **Feature Branch Chain** over Stacked-to-main, but the orchestrator must ask before apply per `ask-on-risk`.

### Suggested Work Units

| Unit | Goal | Likely PR | Focused test command | Runtime harness | Rollback boundary |
|------|------|-----------|----------------------|-----------------|-------------------|
| 1 | Decisions confirmed + skeleton packages + archtest import-graph guard (Phase 1) | PR 1 | `go test ./internal/archtest/...` | N/A — guard has no runtime behavior | Revert empty skeleton packages + `deps_test.go`; nothing else depends on it yet |
| 2 | Store interfaces, migrations, sqlc, SQLite backend, conformance suite (Phase 2) | PR 2 | `go test ./internal/store/...` | `t.TempDir()` SQLite migration integration test | Revert `internal/store/*`; nothing outside it imports yet |
| 3 | Tokens, password KDF, bootstrap, auth middleware (Phase 3) | PR 3 | `go test ./internal/auth/...` | N/A — pure unit tests, injected `now func()` | Revert `internal/auth/*`; only unbuilt `internal/api` would consume it |
| 4 | `internal/server` composition root + `aliasdeck serve` (Phase 4) | PR 4 | `go test ./internal/server/...` | In-process start/shutdown test, ephemeral listener | Revert `internal/server/*` + `cmd/aliasdeck/serve.go`; root command registration reverts together |
| 5 | API router, middleware, CRUD handlers, error shape, OpenAPI (Phase 5) | PR 5 | `go test ./internal/api/...` | `httptest.NewServer` CRUD round trip | Revert `internal/api/*`; `server.Run` router wiring reverts in the same commit |
| 6 | Sync resolution + `GET /api/v1/sync` (Phase 6) | PR 6 | `go test ./internal/sync/... ./internal/api/...` | `httptest` sync handler + byte-identity fixture | Revert `internal/sync/*` and `internal/api/sync.go`; other routes unaffected |
| 7 | `ServerSource`, URL guard, credentials file, `Doctor`/`UnfilteredResolver` (Phase 7) | PR 7 | `go test ./internal/source/... ./internal/config/...` | `httptest.NewServer` scripted `ServerSource` integration | Revert `internal/source/{server,url}.go`, `internal/config/credentials.go`; `FileSource`/`GitSource` unaffected |
| 8 | `login`/`register`/`logout`, server-aware `status`/`doctor`/`edit`/`uninstall` (Phase 8) | PR 8 | `go test ./internal/app/...` | Full in-process serve→login→register→sync integration test | Revert `internal/app/{login,register,logout}.go` and server-aware branches; `resolveSource`'s server arm reverts with it |
| 9 | Byte-identity/full verification, archtest sweep, 25 MB CI gate, docs (Phases 9–10) | PR 9 | `make check && make cover` | CI six-target build + size measurement (N/A locally) | Revert CI/goreleaser/docs changes; no Go behavior depends on them |

Base ordering if Feature Branch Chain is chosen: PR1 → PR2 → PR3 → PR4 → PR5 → PR6 → PR7 → PR8 → PR9, each based off the immediately preceding PR branch. PR7's URL/credentials half (7.1–7.4) has no server-side dependency and MAY be developed off PR1 in parallel, then rebased onto PR6 before its ServerSource half (7.5–7.9) lands. If Stacked-to-main is chosen, land in the same order directly to main — each layer's tests only need earlier layers, never later ones.

## Confirmed No-Change (per design.md and orchestrator instructions)

- `internal/domain`, `internal/validate`, `internal/renderers`: no edits planned. Flag loudly if any task below is found to require one — the PowerShell case-insensitive name/duplicate scenarios in the `config-source` delta already exist from Milestone 3 (`internal/validate/name.go:83-91`, `validate.go:180-186`); this milestone only extends the *rule* to cover `ServerSource` output, not the validation code itself.

## Phase 1: Open Decisions & Foundation Skeleton

- [x] 1.1 **Open item**: confirm `login`/`logout` semantics before Phase 8 — `login` authenticates the operator only (session stored outside `config.yaml`), `logout` clears the local session only and never contacts the server. Record the decision (or a correction) in `design.md` before Phase 8 starts. **Confirmed as proposed** — recorded as design decision 17.
- [x] 1.2 **Open item**: confirm `goose` vs `golang-migrate` before Phase 2 — record the choice in `design.md`; note that the refusal-to-run-against-a-newer-database behavior is ours regardless of runner, so this choice does not gate correctness, only ergonomics. **Confirmed as proposed** — decision 5 annotated.
- [x] 1.3 Add go.mod deps per 1.2: `modernc.org/sqlite`, the chosen migration runner, `golang.org/x/crypto` (argon2), `sqlc` as a build-time tool; `chi` only if middleware ergonomics justify it. Added `modernc.org/sqlite`, `github.com/pressly/goose/v3`, `golang.org/x/crypto` as direct `go.mod` requires. `sqlc` is invoked via version-pinned `go run` (not a `go.mod` `tool` directive — see design decision 6's note on why). `chi` deferred to Phase 5, not added.
- [x] 1.4 Create empty skeleton packages `internal/{server,api,auth,store,sync}/doc.go` (no logic) so the import graph exists to test.
- [x] 1.5 Write `internal/archtest/deps_test.go` **now** (design decision 2): `go list -deps` assertions — no `internal/{server,api,auth,store,sync}` package depends on `internal/renderers`; `internal/source`/`internal/app` never depend on `internal/store` or `modernc.org/sqlite`; skip when `go` is absent from PATH. Must stay green from this commit on — every later phase is graded against it. Failure and recovery proven twice (both assertions) — see apply-progress.
- [x] 1.6 **Open item**: build a minimal `aliasdeck serve` stub (prints "not implemented", imports `modernc.org/sqlite` only); measure binary size vs the ~8 MB baseline and the 25 MB budget; record the delta. If already over budget at this stub stage, escalate to the maintainer before continuing. **Not over budget** — see apply-progress for measured baseline/stub/delta/headroom.

## Phase 2: Server Persistence (`internal/store`)

- [x] 2.1 Write `internal/store/{store,errors}.go` — `Store`, `AliasRepo`, `DeviceRepo`, `ProfileRepo`, `TokenRepo`, `OperatorRepo`; `ErrNotFound`/`ErrConflict`; no driver type in any signature. **Post-review correction**: added a third sentinel, `ErrInvalidReference`, for a dangling foreign-key reference distinct from a name collision — design decision 18.
- [x] 2.2 RED: `internal/store/storetest/conformance.go` — `Run(t, newStore)`: CRUD fidelity, `ErrNotFound`/`ErrConflict`, cascade behavior, deterministic list ordering, cancelled-ctx writes nothing, two concurrent `ConsumeEnrollment` calls yield exactly one device. Also includes the 2.8 SQL-metacharacter round-trip case. Unconsumed until 2.7 wires a real backend — see apply-progress for how its RED→GREEN transition was actually exercised. **Post-review correction**: closed coverage gaps a bounded review found — `Update()` colliding with a *different* existing row's name (alias and profile), missing-id `Update`/`Delete` and duplicate-name `Create` on `ProfileRepo`, a duplicate `tokens.lookup`, `alias_devices`/`device_profiles` cascade in both directions (previously only `alias_profiles` on profile-delete was covered), a multi-table write failing partway through and rolling back fully (not just a pre-transaction context cancellation), an already-expired enrollment token, and `ErrInvalidReference` on a dangling `ProfileIDs`/`DeviceIDs` entry — see apply-progress.
- [x] 2.3 RED: `internal/store/migrate_test.go` — empty-DB migration, idempotent re-run, refusal to start when the DB schema is newer than the binary (threat matrix: migration execution), plus a transactional-rollback-on-failure case (spec scenario, not separately listed as a task but required by server-persistence spec.md). `t.TempDir()` database files, no sleep.
- [x] 2.4 GREEN: `internal/store/migrations/0001_init.sql` — `schema_migrations` (goose-managed version table, custom name via `goose.WithTableName`), `operators`, `profiles`, `aliases`, `alias_profiles`, `alias_devices`, `devices`, `device_profiles`, `tokens`.
- [x] 2.5 GREEN: `internal/store/migrate.go` — runner over `embed.FS` via `goose.NewProvider` (library mode, decision 5), one transaction per file (goose default), `context.WithTimeout(ctx, 30s)` around the whole run (bounded op), newer-DB refusal via `ErrSchemaNewer`.
- [x] 2.6 Write `internal/store/sqlitestore/query.sql` + `sqlc.yaml`; ran `sqlc generate` via `go run github.com/sqlc-dev/sqlc/cmd/sqlc@v1.29.0 generate`; generated code checked in (CI drift check wiring is task 10.3, out of this batch's scope). `query.sql` itself is unchanged by the post-review correction pass — the fixes below are Go-side (`convert.go`, `store.go`), not SQL text — so no regeneration was needed.
- [x] 2.7 GREEN: `internal/store/sqlitestore/*.go` — one `*sql.DB`, `SetMaxOpenConns(1)`, `journal_mode=WAL`/`busy_timeout=5000`/`foreign_keys=on` via DSN pragmas (bounded op); `ctx` first param on every method; wired `storetest.Run(t, newStore)` in `sqlitestore_test.go`. **Post-review corrections**: (a) `timeFormat` (`convert.go`) was `time.RFC3339Nano`, which trims trailing-zero fractional digits and is therefore not fixed-width — `expires_at > ?` and any future `ORDER BY` over a TEXT timestamp column could disagree with chronological order across a whole-second boundary; replaced with a fixed nine-digit-fraction layout, regression-tested in `convert_test.go`. (b) `mapWriteError` collapsed every `SQLITE_CONSTRAINT_*` code to `ErrConflict`; it now checks `SQLITE_CONSTRAINT_FOREIGNKEY` first and returns `ErrInvalidReference` (decision 18). (c) `Open` now creates/tightens the database file to `0600` before the driver touches it, and its WAL/SHM sidecars once they exist (decision 19). (d) `doc.go`'s "skeleton only, Phase 1" comment, stale since this phase landed, removed.
- [x] 2.8 RED+GREEN: threat matrix (SQL construction) — `testAliasNameWithSQLMetacharactersRoundTrips` in `storetest/conformance.go`: an alias name containing `'; DROP TABLE` round-trips as literal text through parameterized queries. Proven to fail against a deliberately concatenated (injection-vulnerable) write path — see apply-progress.
- [x] 2.9 Verify: `go test ./internal/store/...` green; `deps_test.go` (1.5) still green (including a deliberately-broken-store proof, not just the passing run). **Post-review correction**: `storetest`'s `ConsumeEnrollmentIsAtomic` cannot fail against a non-atomic `ConsumeEnrollment` — `Open`'s `SetMaxOpenConns(1)` fully serializes every goroutine's transaction through the connection pool before the SQL guard is ever exercised concurrently. Added two SQLite-specific regression tests in `sqlitestore/tokens_test.go` that bypass that serialization on purpose: one races real concurrent connections naturally, one forces the exact interleaving via a test-only seam (`afterEnrollmentTokenRead`). Mutation-tested against two variants — see apply-progress for the exact mutations, both outputs verbatim, and the one mutation shape that, after direct empirical verification against this database engine's WAL semantics, could not be made to fail and is reported as such rather than papered over.

## Phase 3: Server Auth (`internal/auth`)

- [x] 3.1 RED: `internal/auth/token_test.go` — mint/parse/verify, injected `now func()`; wrong secret, expired, revoked, wrong kind for the route.
- [x] 3.2 GREEN: `internal/auth/token.go` — wire form `ad<k>_<lookup>.<secret>`; `lookup` UNIQUE-indexed plain text, `secret` sha256-hashed, `subtle.ConstantTimeCompare` (threat matrix: token handling). **Post-review correction**: `ConsumeEnrollment`'s kind guard (`parsed.Kind != store.TokenKindEnrollment`) was never exercised by any test — the one that looked like it covered it (`"adx_wrong.wire"`) fails inside `Parse` on the unknown kind byte, never reaching the guard. Added `TestConsumeEnrollmentRefusesNonEnrollmentKindTokens` presenting well-formed device-kind and session-kind wire tokens, and made `fakeTokenRepo.ConsumeEnrollment` mirror `query.sql`'s `kind = 'enrollment'` filter (it previously accepted any kind, more permissive than production in exactly the dimension being tested). Mutation-verified: deleting the guard now fails the new test — see apply-progress.
- [x] 3.3 RED: `internal/auth/password_test.go` — argon2id hash/verify round trip, wrong password rejected.
- [x] 3.4 GREEN: `internal/auth/password.go` — `golang.org/x/crypto/argon2` for the operator password only. **Post-review correction**: (a) `VerifyPassword` parsed `m=`/`t=`/`p=` out of a stored hash and rejected only zero — a well-formed hash with `m=4000000000` (~4 TB) reached `argon2.IDKey` uncapped and hung the process allocating memory until it was SIGTERM-killed, worse than a panic since no recovery middleware can catch an allocation that never returns. Added `argon2MaxMemory`/`argon2MaxTime`/`argon2MaxThreads` (8× the shipped parameters — real headroom, not the four-orders-of-magnitude gap the reported hash exploited) and a bound check before `argon2.IDKey` is ever called. (b) Extended `TestVerifyPasswordRefusesMalformedHashWithoutPanicking`'s coverage: every case in that original table failed at the very first check (`len(parts) != 5 || parts[0] != hashFormat`), leaving the version parse, parameter parse, zero/oversized-parameter guards, and salt/hash base64 decodes dead code as far as the suite was concerned. Added `TestVerifyPasswordRefusesCorruptionPastTheHeader` with well-formed, five-part hashes corrupting exactly one component past the header. Mutation-verified each branch by deleting it and confirming a failure, with one honest exception: the parameter-`Sscanf`-error guard cannot be independently killed, because any `Sscanf` failure necessarily leaves at least one of `memory`/`time`/`threads` at its Go zero value, which the very next check (zero-parameter guard) already rejects — see apply-progress for the full mutation table.
- [x] 3.5 RED: `internal/auth/bootstrap_test.go` — first start on an empty DB creates one operator and prints a generated password once, never logged; `ALIASDECK_ADMIN_PASSWORD` honored; subsequent starts don't reprint (bounded op: zero stdin reads).
- [x] 3.6 GREEN: `internal/auth/bootstrap.go` — `crypto/rand` password generation, zero stdin reads. **Post-review correction**: `ALIASDECK_ADMIN_PASSWORD` was accepted unvalidated — a single-character password worked with no rejection and no warning. Added `validateAdminPassword` (rejects empty/whitespace-only and anything under `minAdminPasswordLength = 12`, naming the variable in the error) and `ErrWeakAdminPassword`; never applied to a generated password. Recorded the fixed `admin` bootstrap username as design decision 20 (previously discoverable only by reading an unexported constant). **Post-review correction (bounded-review, second pass, design decision 22)**: `Bootstrap`'s only delivery path for a generated password was its `out io.Writer` — correct for a console, but under a systemd `serve` deployment that writer is stdout wired to the journal, which `server-auth/spec.md` explicitly forbids. Added a `passwordFilePath string` parameter: empty preserves the original console-printing behavior verbatim (every pre-existing test call site passes `""`); non-empty makes `Bootstrap` write the password atomically at `0600` (`writeBootstrapPasswordFile`, mirroring `internal/state.Save`'s existing temp-file-then-rename pattern) and print only that path. Delivery was also reordered to happen *before* the operator row is created, so a failed delivery leaves no operator behind for a clean retry. `internal/auth` itself performs no terminal detection — that stays the caller's job (`cmd/aliasdeck/serve.go`'s `isTerminalWriter`) — so this package's dependency surface is otherwise unchanged. New tests: `TestBootstrapWritesGeneratedPasswordToFileWhenPathGiven`, `TestBootstrapDoesNotWritePasswordFileOnRestart`, `TestBootstrapFailsCleanlyAndCreatesNoOperatorWhenPasswordFileCannotBeWritten`.
- [x] 3.7 RED: `internal/auth/middleware_test.go` — per-route required token kind enforced; a device token on an operator route is refused (threat matrix: HTTP routing); expired/revoked tokens rejected.
- [x] 3.8 GREEN: `internal/auth/middleware.go`.
- [x] 3.9 RED+GREEN: threat matrix (token handling) — a replayed enrollment token is refused end-to-end; a second `register` with an already-consumed token yields no second device token. Mutation-tested against the four required mutations (constant-time compare, middleware kind check, `crypto/rand`, expiry check) — see apply-progress for verbatim results, including the one (constant-time vs. `bytes.Equal`) that produces no test failure, honestly reported as a timing property this suite cannot observe.

## Phase 4: Server Runtime (`internal/server`, `cmd/aliasdeck/serve.go`)

- [x] 4.1 RED: `internal/server/server_test.go` — `Run(ctx)` migrates before accepting connections; refuses on a newer schema; health endpoint requires no auth; shutdown drains an in-flight request within bound then exits. Migration-ordering is proven by a single shared event channel both `OpenStore` and `Listen` send to from `Run`'s own goroutine — receive order is call order, so swapping the two lines in `Run` fails the test (verified). Newer-schema refusal is exercised against a real SQLite file (`sqlitestore.Open` then a forged `schema_migrations` row), asserting both `errors.Is(err, store.ErrSchemaNewer)` and that `Listen` was never called. Health-endpoint-requires-no-auth runs a real `Run` on an ephemeral listener and does a plain unauthenticated `GET /api/v1/health`.
- [x] 4.2 GREEN: `internal/server/{server,config}.go` — `newHTTPServer` constructs the bounded `http.Server` (`ReadHeaderTimeout:5s, ReadTimeout:15s, WriteTimeout:30s, IdleTimeout:60s, MaxHeaderBytes:64<<10`), asserted directly on the constructed value by `TestNewHTTPServerAppliesEveryBound`. `Run` installs `signal.NotifyContext(ctx, os.Interrupt, syscall.SIGTERM)` and, on `ctx.Done()`, calls the unexported `shutdown()` helper: `srv.Shutdown(ctx)` bounded by `Config.ShutdownTimeout` (production default 10s via `defaultShutdownTimeout`, injectable in tests so the non-draining-request test doesn't wait a real 10s), then `srv.Close()` unconditionally, then waits for the `Serve` goroutine to return. Two dedicated tests: a successful drain (`TestShutdownDrainsInFlightRequestThenReturns`) and a request that never finishes, proving `Close()` — not `Shutdown()`'s own deadline — is what forcibly terminates it (`TestShutdownClosesUnconditionallyWhenARequestNeverFinishes`; see apply-progress for why the first draft of this test could not detect a skipped `Close()` and how it was corrected). **Post-review corrections (bounded-review, second pass)**: (a) a stop signal arriving while `OpenStore` (up to the 30s migration bound) or `Bootstrap` was still running propagated out as a wrapped error, indistinguishable in logs from a real startup failure — `Run` now checks `ctx.Err()` after either call fails and returns `nil` when the caller itself cancelled, never masking a genuine failure since only cancellation flips `ctx.Err()`. Covered by `TestRunReturnsNilWhenCancelledDuringOpenStore` and `TestRunReturnsNilWhenCancelledDuringBootstrap`. (b) `defer st.Close()` was verified by nothing — deleting it left the package green; `fakeStore.isClosed()` existed but was called from nowhere. Now exercised by `TestRunClosesStoreAfterReturning`. (c) `Run`'s own `auth.Bootstrap` call had both branches uncovered (the fixed `fakeStore` always reported one operator, so `Bootstrap` no-op'd every time); `newFakeStoreWithEmptyOperators`/`newFakeStoreWithOperatorCreateError` plus `TestRunBootstrapsOperatorOnEmptyStore`/`TestRunWrapsBootstrapErrorFromOperatorCreate` now drive both. (d) the five `http.Server` bounds' rationale (design decision, Bounded Operations table) was values with no stated provenance; the doc comment above the bound constants now says plainly they are conventional defaults, not measurements, matching `GitTimeout`'s house style of recording either a measurement or an honest absence of one.
- [x] 4.3 RED: zero-stdin-prompt test — `TestRunNeverReadsStdin` replaces `os.Stdin` with the read end of a pipe whose write end is never written to or closed, so any real read hangs forever; `Run` must still reach `Listen` and return on `ctx` cancellation within a bounded test timeout. Deliberately stronger than literally closing stdin (which would let a stray read return `io.EOF` silently and prove nothing) — matches this project's own precedent, "a stdin prompt on a pipe that never delivered."
- [x] 4.4 GREEN: confirmed via `auth.Bootstrap(ctx, st, cfg.Getenv, cfg.Stdout)` wiring from Phase 3 — `Bootstrap`'s signature has no `io.Reader` parameter at all, so there is structurally nothing in `Run`'s call path that can read stdin.
- [x] 4.5 Created `cmd/aliasdeck/serve.go` — flags (`--addr`, `--db`) and `os.Getenv`/`cmd.OutOrStdout()` resolve to a `server.Config`; the default database path comes from `config.Base` + `server.db` (no change to `internal/config` itself). It is the only file in `main` importing `internal/server`; already registered in `cmd/aliasdeck/root.go` from the Phase 1 stub (`newServeCmd()` was already in `AddCommand`), so no further root.go edit was needed. **Post-review corrections (bounded-review, second pass)**: (a) `defaultServeAddr` was `":8080"` — an empty host, which binds every interface; a zero-flag `aliasdeck serve` was reachable from the LAN behind nothing but a bootstrap password printed once, and contradicted `ServerSource`'s own loopback-or-HTTPS refusal (decision 13). Changed to `"127.0.0.1:8080"` (decision 21); `--addr`'s help text now states the default and what widening it means. (b) `resolveServeDBPath`'s `os.MkdirAll(base, 0o700)` did not match `init`/`config.Write`/`state.Save`'s `0o755` for this same shared directory, and the doc comment claimed otherwise; changed to `0o755` and the comment now names the three call sites it matches. (c) the bootstrap password's only delivery path was `cmd.OutOrStdout()`, which under systemd's default is the journal, not a console (`server-auth/spec.md`'s "MUST NOT ... any log", violated on the one deployment `server-runtime/spec.md` requires supporting); added `isTerminalWriter` (`github.com/mattn/go-isatty`, now a direct dependency) and `bootstrapPasswordFilePath`, wired through a new `server.Config.BootstrapPasswordFile` field into `auth.Bootstrap`'s new parameter (decision 22).
- [x] 4.6 Verify: `internal/archtest/deps_test.go` (1.5) and `go vet ./...` both green with real server wiring present (`internal/server` now imports `internal/auth`, `internal/store`, `internal/store/sqlitestore` — none of it imports `internal/renderers`). `go test -count=1 ./...` green; manual smoke of `./aliasdeck serve` confirmed a one-time bootstrap password, `GET /api/v1/health` returning `200 {"status":"ok"}`, and a clean `SIGTERM` exit (code 0). See apply-progress for the four required mutation results (all confirmed) and the six cross-compile sizes against the 25 MB budget. **Post-review correction (bounded-review, second pass)**: `TestRunHealthEndpointRequiresNoAuthentication` asserted only the status code; `handleHealth`'s own comment promises no schema version, build info, or path leaks, and nothing enforced it. The test now asserts the exact body (`{"status":"ok"}\n`) and `Content-Type`. Added `cmd/aliasdeck/serve_test.go` (previously no test existed for this file at all): flag wiring, `resolveServeDBPath`'s precedence and idempotency, and both terminal-detection helpers — ephemeral only, no fixed port bound and no long-running process started, per the incident that opened this correction pass. See apply-progress for the full mutation table covering findings 3–6 and 9.

## Phase 5: Server API (`internal/api`)

- [x] 5.1 RED: `internal/api/router_test.go` — explicit `[]route{method,pattern,handler}` slice; a route missing its required-token-kind declaration fails registration (threat matrix: HTTP routing). `route` also carries a `Public` flag (design decision 23): a route must declare exactly one of `Public` or a valid `RequiredKind`; declaring neither fails registration in `newRouter` itself (`TestNewRouterFailsRegistrationForRouteMissingRequiredKindDeclaration`, `TestNewRouterFailsRegistrationForUnknownRequiredKind`), and `TestHealthRouteIsReachableWithoutAuthentication` asserts the real production `routes()` table's named `healthMethod`/`healthPattern` route specifically, by name, unauthenticated. Mutation-verified — see apply-progress.
- [x] 5.2 GREEN: `internal/api/router.go` — route slice (`routes()`, currently just `GET /api/v1/health`, `Public: true`, decision 23), `http.TimeoutHandler(mux, 20s, timeoutBody)` wrapping the whole mux (bounded op), `timeoutBody` using the same error shape as 5.6. **Scope note**: wiring this router into `internal/server.Run` (replacing `handler.go`'s Phase 4 direct wiring) is deliberately deferred — it depends on the CRUD/auth handlers Phase 5's second half (5.7–5.10) adds, and is out of this batch's assigned scope (5.1–5.6 only).
- [x] 5.3 RED: `internal/api/middleware_test.go` — `http.MaxBytesReader(w, r.Body, 1<<20)` rejects an oversized body before it is fully read (bounded op). Proven with an `infiniteReader` body source: the handler's `io.Copy(io.Discard, r.Body)` must return a `*http.MaxBytesError` and the test itself bounds the wait at 2s, so removing the wrapper makes the read against the infinite source hang past that bound and fail loudly instead of the read-to-completion-then-check-length pattern that cannot detect this property. Mutation-verified — see apply-progress.
- [x] 5.4 GREEN: `internal/api/middleware.go` — `withMaxBytes`.
- [x] 5.5 RED: `internal/api/errors_test.go` — one `{"error":{"code","message","details"}}` shape across endpoints, asserted against two distinct simulated endpoints producing different store sentinels. `ErrInvalidReference` mapping asserted at exactly 422 (Unprocessable Entity), not 409 (design decision 18). Internal error text never appears in the body. Mutation-verified — see apply-progress.
- [x] 5.6 GREEN: `internal/api/errors.go` — `writeError`/`writeStoreError`; `ErrNotFound`→404, `ErrConflict`→409, `ErrInvalidReference`→422.
- [x] 5.7 RED: `internal/api/{aliases,profiles,devices}_test.go` — unauthenticated CRUD rejected; authenticated create+list round trip; `validate.Command`/`Description` failures are `400`; `validate.Name` warnings over `serverValidationShells` never block (design decision 16). Every handler test runs through the real `NewRouter`/`RequireKind`/`withMaxBytes` chain against a new in-memory `fakeStore` (`internal/api/fakestore_test.go`, a `store.Store` double — not a shortcut around the router) so a missing auth guard or body bound would fail these tests too, not only `router_test.go`'s synthetic ones.
- [x] 5.8 GREEN: `internal/api/{aliases,profiles,devices}.go`. `validateAliasWrite` calls `validate.Command`/`validate.Description` (blocking, 400) and never calls `validate.Name`; `nameWarnings` runs `validate.Name` per shell in `serverValidationShells` and returns its issues as informational `nameWarnings` on the create/update response, never as a rejection. Devices have no `POST` route — `DeviceRepo` has no `Create` (a device is born only through `devicesRegisterPattern`'s enrollment exchange, 5.10).
- [x] 5.9 RED: `internal/api/auth_test.go` — enrollment-token generation/consumption, login/logout, device token rotation/revocation. Includes the required threat-matrix case verbatim: `TestReplayedEnrollmentTokenIsRefusedEndToEnd` registers once, replays the same consumed token, and asserts both the second call's rejection and that exactly one device exists afterward (no second device token minted).
- [x] 5.10 GREEN: `internal/api/auth.go` — `POST /api/v1/auth/login` (Public; body credential, not a bearer token), `POST /api/v1/auth/logout` (session), `POST /api/v1/enrollment-tokens` (session), `POST /api/v1/devices/register` (Public; authenticates itself via `auth.ConsumeEnrollment` against a bearer enrollment token, since `RequireKind` only verifies and this call must also atomically consume). Device token rotation (`POST /api/v1/devices/{id}/token`) and revocation (`POST /api/v1/devices/{id}/revoke`) landed in `internal/api/devices.go` since both operate on an existing device id, not the auth-lifecycle routes; both revoke every device-kind token for the id via `Tokens().RevokeSubject` before/instead of minting a replacement, satisfying "Immediate Device Revocation" and "Device Token Rotation" (server-auth spec).
- [x] 5.11 Wrote `docs/openapi.yaml` covering every one of the 22 declared routes (health, the spec route itself, 4 auth/enrollment routes, 5 alias routes, 5 profile routes, 7 device routes).
- [x] 5.12 GREEN: `internal/api/openapi.go` — embed and serve `GET /api/v1/openapi.yaml`, Public. `go:embed` cannot reference a path outside its own package directory (the same constraint design decision 5 already hit for migrations), so the embedded copy lives at `internal/api/openapi.yaml`; `TestEmbeddedOpenAPISpecMatchesDocsCopy` (5.13) keeps it byte-identical to `docs/openapi.yaml`, the same drift-check discipline as the sqlc-generate check (decision 6).
- [x] 5.13 RED+GREEN: `internal/api/openapi_coverage_test.go` — `TestOpenAPIDocumentsExactlyTheRegisteredRoutes` checks both directions in one test by construction (registered-but-undocumented in the first loop, documented-but-not-registered in the second), naming the offending "METHOD pattern" in either case. Mutation-verified: an added, undocumented `GET /api/v1/undocumented` route fails naming exactly that route; deleting the registered `DELETE /api/v1/profiles/{id}` route while leaving it in `docs/openapi.yaml` fails naming exactly that route — see apply-progress for verbatim output.
- [x] 5.14 **Bounded-review addition**: bound concurrent password verification (design's Bounded Operations table, "Concurrent password verification"). Implemented as `internal/api/auth.go`'s unexported `loginConcurrency = 4` and a package-level `loginSem chan struct{}` (per-`*api` instance, sized in `NewRouter`) that `handleLogin` acquires immediately before, and releases immediately after, its call to `verifyPassword` (a package-level seam over `auth.VerifyPassword` used only so the concurrency test can observe/hold calls in flight without paying argon2id's real cost). Excess attempts queue on the channel send, bounded in turn by the existing `http.TimeoutHandler` 20s bound (5.2) — no second timeout was added. Confirmed per design decision 24: the semaphore lives in `internal/api/auth.go`, not `internal/auth`. `TestLoginConcurrencySemaphoreBoundsConcurrentVerifyPasswordCalls` proves the bound is real, not incidental: it fires `loginConcurrency*3` concurrent real HTTP requests at a real `httptest.Server`, confirms exactly `loginConcurrency` reach the instrumented `verifyPassword` and block there, then asserts a bounded 200ms window during which no further call enters — the exact trap flagged for this milestone ("a concurrency test where the harness itself serializes callers proves nothing") is avoided by launching genuinely concurrent goroutines against a real listener, not a single-threaded call sequence. Mutation-verified: removing the semaphore acquire/release around `verifyPassword` fails this test (a 5th call enters while 4 are already held open) — see apply-progress for verbatim output.
- [x] 5.15 **Not originally a numbered task, added per orchestrator instruction**: wired `internal/api.NewRouter` into `internal/server.Run`, replacing the Phase 4 `handler.go` stub (deleted — its `GET /api/v1/health` wiring is now `(*api).routes()`'s own `Public` entry, decision 23). `internal/server/server_test.go`'s `TestRunHealthEndpointRequiresNoAuthentication` (Phase 4) required no changes and stays green, confirming the health route is still reachable unauthenticated through the real router.
- [x] 5.16 **Bounded-review correction pass (four-lens, third pass over Phase 5)**: 1 CRITICAL + 6 WARNING findings, all fixed in this batch. **CRITICAL 1** (5.14's own semaphore): the acquire was a bare `a.loginSem <- struct{}{}` send that never observed `r.Context()` cancellation, contradicting design decision 24's own claim that overflow was "bounded in turn by the handler's existing `http.TimeoutHandler` 20s bound"; fixed to `select { case a.loginSem <- struct{}{}: case <-r.Context().Done(): ... }`, decision 24 and the Bounded Operations table corrected to state the claim was false as shipped. **WARNING 2**: `internal/auth/middleware.go`'s `RequireKind` hardcoded `http.Error` (plain text), so 18 of 22 guarded routes answered `401` in a different shape than the rest of this JSON API; inverted via a new `Refuse func(http.ResponseWriter)` parameter (nil falls back to the old default), with `internal/api/router.go` supplying `writeUnauthorized` — recorded as design decision 25. **WARNING 3**: `handleAliasesGet/Update/Delete`, `handleProfilesGet/Update/Delete`, `handleDevicesList/Get/Update/Delete` were only ever exercised by unauthenticated-rejection tests (401 before the handler body ran); added authenticated round-trip tests for all ten, each mutation-verified against the exact hypothetical defect named in the review (id-ignoring Get, body-discarding Update, no-op Delete). **WARNING 4**: `handleDevicesRegister`'s post-`ConsumeEnrollment` token `Create` and `handleDevicesRotateToken`'s post-`RevokeSubject` token `Create` are each a second, non-atomic write; accepted and documented (not compensated/reordered) as design decision 27, with the orphaned/left-bare device's id now named in the error response and a working recovery path (rotate-token; safe retry) proven by test. **WARNING 5**: `validate.MaxAliases` — assumed by design decision 4 to already bound the alias set — was never enforced from the API's create path; `handleAliasesCreate` now checks it directly (`codeTooManyAliases`, 400); list-endpoint boundedness otherwise recorded as an accepted, owned decision (design decision 26), not silently unbounded. **WARNING 6**: `decodeJSON` now calls `dec.DisallowUnknownFields()` on every route, including the two unauthenticated ones; verified against the full existing suite with no legitimate request breaking. **WARNING 7**: `TestLoginRejectsWrongPassword`'s doc comment asserted a security property (no timing signal between "no such operator" and "wrong password") that does not hold — an unknown username returns before ever reaching the KDF; corrected to state the real, accepted-because-`admin`-is-published property (design decision 20), explicitly rejecting timing-equalization as a fix since it would let an attacker exhaust the login semaphore with garbage usernames. See apply-progress for the CRITICAL 1 test-construction discussion (network-cancellation-propagation race avoided by calling `handleLogin` directly with a caller-owned cancellable context) and every mutation's verbatim before/after output.

## Phase 6: Server Sync (`internal/sync`, `internal/api/sync.go`)

- [x] 6.1 RED: `internal/sync/resolve_test.go` — `Resolve(ctx, store, dev)` filters identically to local `FileSource` resolution on a shared fixture (no SQL-side filtering, design decision 4). `TestResolveMatchesLocalFileSourceResolution` shares ONE fixture (`fixtureYAML`, a real aliases.yaml document) between both paths: `source.FileSource.Resolve` (production code, unmodified) reads it from a temp file; `sync.Resolve` reads the same bytes parsed via `config.ParseAliases` (the identical parser `FileSource` itself calls) through a fake `store.Store` whose `Aliases().List` returns that slice. Four devices exercise platform, shell, profile, and the always-excluded disabled alias in one table. `TestResolveAppliesDevicePinning` covers `DeviceIDs`, the one targeting dimension `aliases.yaml`'s schema cannot express, directly in domain terms. `TestResolveWrapsAliasListError` proves a store failure surfaces rather than returning an empty success. Mutation-verified: forcing every alias's `Platforms` to `nil` inside `sync.Resolve` before calling `domain.Resolve` failed 2 of 4 device subtests immediately — see apply-progress for the verbatim diff output.
- [x] 6.2 GREEN: `internal/sync/resolve.go` — loads the full alias set via `store.AliasRepo.List` (enabled and disabled alike, per that method's own documented contract) and calls `domain.Resolve(dev, aliases)` directly; no targeting comparison of any kind lives in this package.
- [x] 6.3 RED: `internal/api/sync_test.go` — `TestSyncResolvesAndRespondsWithTheDesignatedShape` decodes the response into a generic `map[string]any` (not the production `syncResponse` struct) specifically so an added, undeclared field cannot be silently discarded by unmarshalling, then asserts the alias entry's key set is exactly `{name, command, description}` and explicitly fails if `id` is present. Threat matrix: sync response contract. Mutation-verified: adding `ID string \`json:"id"\`` to `syncAlias` failed both this test and `TestGoldenSyncResponse` — see apply-progress.
- [x] 6.4 GREEN: `internal/api/sync.go` — `GET /api/v1/sync` handler, guarded by `RequiredKind: store.TokenKindDevice` (the project's first device-gated route). Reads `platform`/`shell` from query parameters (design decision 9's exact shape), fetches the device row for its server-owned `ProfileIDs` (client-supplied profile data is never read — server-sync spec, "Client Owns Platform/Shell; Server Owns Profile Membership"), calls `sync.Resolve`, then persists the request's own platform/shell and stamps `last_seen_at`/`last_sync_at` via `Devices().Touch` (design decision 10) before responding. An empty or unrecognized platform/shell is `400` (`codeInvalidRequest`) naming the full valid set by name, never a silent default. **Fixed a latent fake-store gap found while testing this**: `fakeDeviceRepo.Touch` (`internal/api/fakestore_test.go`) accepted its `at time.Time` parameter and silently dropped it, never writing `LastSeenAt`/`LastSyncAt` — a fake strictly more permissive than the real `sqlitestore` backend, and exactly the "a fake more permissive than the real store" failure shape this project has hit before. No test exercised `Touch` before this phase (no route called it). Fixed to actually persist both fields from `at`. Mutation-verified: an unknown shell silently defaulting to `zsh` instead of returning `400` failed `TestSyncRejectsUnknownShellNamingTheValidSet` — see apply-progress.
- [x] 6.5 Golden: `internal/api/testdata/{sync_response,error_response}.golden`, generated via `internal/api/golden_test.go`'s own `-update` flag (mirrors `internal/renderers/golden_test.go`'s convention). `sync_response.golden` pins a full `GET /api/v1/sync` response over a real router with a fixed clock; `error_response.golden` pins sync's own actionable 401 (task 6.6). Comparison re-marshals both sides through `encoding/json` with sorted keys before comparing, so the check is insensitive to incidental key-order changes while still catching every field-level regression.
- [x] 6.6 RED+GREEN: `internal/api/sync_test.go`'s `TestSyncFailsWithARevokedDeviceTokenWithAnActionableMessage` and `TestSyncFailsWithARotatedDeviceTokenWithAnActionableMessage` — both run over real HTTP against the real production `POST /devices/{id}/revoke` and `POST /devices/{id}/token` routes, closing the deferral `devices_test.go`'s `TestDevicesRotateTokenInvalidatesThePreviousToken` (Phase 5) left explicit in its own comment: *"No device-kind-gated route exists yet in this batch's scope (sync is Phase 6)."* Now one does. **Design decision 28** (added to `design.md`): `internal/api/router.go`'s `route` gained an optional `Refuse auth.Refuse` field (nil falls back to `writeUnauthorized`, decision 25's shape, for every other route unchanged); `syncPattern` is the only route to set it, to `sync.go`'s `writeUnauthorizedDevice` — same status, same Content-Type, same `{"error":{...}}` shape as every other 401, but naming the one recovery action a device has ("register this device again") instead of the word "unauthorized," which the task's own instructions called out as non-actionable. The message stays uniform across every failure `RequireKind`/`auth.Verify` can hit for this one route (missing header, malformed, unknown lookup, wrong secret, wrong kind, expired, revoked) — it does not distinguish which one occurred, preserving the anti-enumeration property decision 25 already established for every other route (threat matrix: token handling). The rotation test also asserts the freshly-rotated token succeeds, proving rotation invalidates only the old credential. Mutation-verified: disabling `auth.Verify`'s revocation check (`internal/auth/token.go`, reverted immediately after) failed both tests — see apply-progress.
- [x] 6.7 **Bounded-review correction pass (four-lens, post-Phase-6)**: 1 CRITICAL + 4 WARNING/informational findings. **CRITICAL 1**: `handleSync` discarded a fully resolved alias set on a transient `Devices().Touch` failure (`writeStoreError` + `return`), contradicting decision 10's own framing of `Touch` as bookkeeping, not resource mutation; fixed to log the failure (`slog.Default().Error`, device id + error) and still serve the resolved aliases with `200` — recorded as design decision 29. New test: `TestSyncServesResolvedAliasesWhenTouchFails`. **WARNING 2**: `handleDevicesDelete` removed a device row without revoking its device-kind token, so a deleted device's own token still authenticated `GET /api/v1/sync` in full, failing only with a `404` (not `401`) because `handleSync` happens to load the device row — a future device-gated route that doesn't would accept it outright; fixed to call `Tokens().RevokeSubject` before `Devices().Delete` (mirroring `handleDevicesRevoke`'s ordering, and design decision 27's retry-safety reasoning) — recorded as design decision 30. New tests: `TestDevicesDeleteRevokesItsToken` (store-level), `TestSyncFailsWithADeletedDeviceTokenWithAnActionableMessage` (end-to-end, device-gated route). **Recorded, not changed**: `sync.Resolve`'s `1 + 2N` query cost (design's Bounded Operations table, new row) — measured, linear, and accepted at `validate.MaxAliases`; `GET /api/v1/sync`'s ~22 MB worst-case response size (Bounded Operations table, new row) — already capped by `validate.MaxAliases` (decision 26, WARNING 5), ownership now stated explicitly rather than left implicit alongside decision 26's operator-only list endpoints. **Rejected**: a claim that `TestResolveMatchesLocalFileSourceResolution` cannot detect a regression in platform targeting — reproduced directly; the claimed "zero test failures" from forcing `Platforms` to `nil` was false (2 of 4 subtests fail, exactly as the existing task 6.1 record states). The underlying concern was still worth closing: `internal/sync/resolve_test.go`'s fixture gained `linuxonly` (platform-restricted, no shell restriction) and `zshonly` (shell-restricted, no platform restriction) so platform and shell each have at least one alias/device pair where that single dimension is the sole discriminator, not merely a field-content difference on an alias that was included either way. Five single-dimension mutations (platform, shell, profile, device pinning, enabled), each applied alone directly in `internal/sync/resolve.go` and reverted immediately after, each failed the suite for that dimension's own reason — see apply-progress for all five verbatim results.

## Phase 7: ServerSource & Credentials (`internal/source`, `internal/config`)

- [x] 7.1 RED: `internal/source/url_test.go` — `ValidateServerURL`: https accepted, loopback http accepted, remote http refused, opt-out accepted, unparseable/non-http scheme rejected.
- [x] 7.2 GREEN: `internal/source/url.go` — `ValidateServerURL(raw, allowHTTP)` (design decision 13); checked at `login` and re-checked on every `sync`.
- [x] 7.3 RED: `internal/config/credentials_test.go` — round trip, `0600`, atomic-write cleanup on failure (`t.TempDir()`).
- [x] 7.4 GREEN: `internal/config/credentials.go` + `CredentialsFile(base)` in `paths.go` (design decision 14).
- [x] 7.5 RED: `internal/source/server_test.go` — `httptest.NewServer` scripted: revision mismatch rejected; oversize body truncated-and-failed via `io.LimitReader(resp.Body, 1<<20)` (bounded op, proven with an endless-body `infiniteReader` so the test cannot pass regardless of the bound — see apply-progress); non-2xx mapped; offline hard error naming the URL, no cache; `Stale` always false; `http.Client{Timeout:30s}`, no retries (bounded op).
- [x] 7.6 GREEN: `internal/source/server.go` — `ServerSource{URL,Token,Client,AllowHTTP}` implementing `Resolve` verbatim + optional `ResolveReporter`.
- [x] 7.7 RED+GREEN: threat matrix (sync response as hostile input, client side) — a hostile server-stored alias is dropped by `validate.FilterValid` identically to `FileSource`/`GitSource`. Shared table (`internal/source/hostile_test.go`) of 17 hostile shapes (shell metacharacters, command substitution, newline/CR in name and command, non-identifier names, POSIX/PowerShell-reserved words, oversized command/description, quote/brace-breaking characters) run through both `ServerSource` and `FileSource`, asserting the identical surviving set. Mutation-verified: see apply-progress.
- [x] 7.8 RED: `internal/app/doctor_test.go` (server-source case) — `Doctor` explains exactly what `FilterValid` dropped, using the unfiltered alias set.
- [x] 7.9 GREEN (same pair as 7.8, design decision 12): added `source.UnfilteredResolver{ ResolveUnfiltered(ctx, dev) (domain.ResolvedConfig, error) }`; `ServerSource` implements it; `internal/app/doctor.go` type-asserts it, mirroring `ResolveReporter` (M3 decision 14). **Refactor note**: extracted `doctorFromContext(ctx, env, dc deviceContext)` so the server-source branch is testable against a fake `source.UnfilteredResolver` without depending on Phase 8's `resolveSource` server-type wiring (task 8.1, not yet landed) — `Doctor` itself is now a thin wrapper calling `loadDeviceContext` then `doctorFromContext`.
- [x] 7.10 **Bounded-review correction pass (four-lens, post-Phase-7)**: 1 CRITICAL + 2 WARNING + 1 SUGGESTION finding, all fixed in this batch. **CRITICAL 1**: `ServerSource`'s HTTP client had no `CheckRedirect`, so Go's default redirect handling — which forwards `Authorization` whenever the redirect target's canonical host:port matches the original request's, regardless of scheme — let a same-host `https://` → `http://` redirect re-send the device token in cleartext, defeating design decision 13's guarantee with one 302 from a hostile or misconfigured server; `ValidateServerURL` never sees it, since it only inspects the configured base URL before the request leaves. Fixed by refusing every redirect outright (design decision 31): `internal/source/server.go`'s `httpClient()` now always constructs a fresh client with `CheckRedirect: refuseRedirect`, wrapping — never mutating — a caller-supplied `Client`'s own `Transport`/`Jar`/`Timeout`; `fetchSync` reports the refusal by name via `errors.Is(err, errRedirectRefused)` rather than folding it into the generic "unreachable" wording. New test: `TestServerSourceRefusesRedirectToADifferentSchemeSameHost` (`internal/source/server_test.go`), modelling both legs with real httptest servers (one TLS, one plain) whose logical request URLs share one host:port string and whose actual dial is routed per-scheme by a test-only `schemeRoutingTransport`, reproducing the reported cleartext-forwarding behavior exactly before the fix and refusing it after. New spec requirement ("Sync Request Never Follows a Redirect") and threat-matrix row ("Same-host redirect on the sync request") added. Mutation-verified: reverting `httpClient()` to its pre-fix form fails the new test naming the exact `Resolve() = nil error` gap the finding reported — see apply-progress. **WARNING 2**: `TestHostileServerAliasNeverBypassesFilterValid` (`internal/source/hostile_test.go`) exercised only one of `hostileAliasCases`' 17 rows as its negative control; the other 16 rows' absence in the main table (`TestHostileServerAliasDroppedIdenticallyToFileSource`) could not distinguish `validate.FilterValid`'s own drop from a malformed row falling out for an unrelated reason. Extended to a table-driven test over all 17 rows, each proving `domain.Resolve` alone keeps it and only `validate.FilterValid` then reports an issue. Mutation-verified: forcing a row to fail `domain.Resolve` (simulating "never arrived intact") is caught by the fixture-broken `t.Fatalf` this extended test adds — see apply-progress. **WARNING 3**: neither atomic-write failure test in `internal/config/credentials_test.go` seeded a real pre-existing `credentials.json` before inducing its failure, so neither proved the other half of what an atomic write guarantees — that existing credentials survive a failed write, not just that no temp file leaks. Added `TestCredentialsSaveFailsWhenTheDirectoryCannotBeWrittenToPreservesExistingCredentials`: seeds a real credentials file, makes its directory unwritable (skipped on Windows, where this permission bit is not reliably enforced — the same reason `state.TestStateSaveFailsWhenTempFileCannotBeCreated`'s own history gives for avoiding this technique when a pre-existing file is not required), induces a failed `SaveCredentials`, and asserts the original bytes and values are unchanged. Mutation-verified: rewriting `SaveCredentials` to write directly to `path` (no temp-file-then-rename) fails this test twice over — no error is returned, and the original `DeviceToken` is clobbered by the failed attempt's — see apply-progress. **SUGGESTION 4**: `ResolveUnfiltered`'s doc comment described only what it omits (`validate.FilterValid`), reading as the *fuller* variant of `Resolve` rather than the deliberately dangerous, unvalidated one; every sibling hostile-input method (`Resolve`, `FileSource`, `GitSource`) already states its input is hostile in its own doc comment. Rewrote `UnfilteredResolver` and `ResolveUnfiltered`'s doc comments (`internal/source/server.go`) to state plainly that the result is unvalidated hostile input that must never reach a renderer, a write path, or any shell-executed output — only `Resolve`'s filtered result may.

## Phase 8: CLI Wiring (`internal/app`, `cmd/aliasdeck`)

- [ ] 8.1 GREEN: `internal/app/context.go` `resolveSource` — add `config.SourceTypeServer` building `*source.ServerSource` from `devCfg.Source.URL`, the credentials file, and `source.allowInsecureHTTP`.
- [ ] 8.2 RED: `internal/app/login_test.go` — per 1.1: authenticates against the server, stores the session outside `config.yaml`/device-token file; `--password-stdin` behind `isInteractive` (bounded op), never a terminal prompt; incorrect password rejected.
- [ ] 8.3 GREEN: `internal/app/login.go` + `cmd/aliasdeck/login.go`.
- [ ] 8.4 RED: `internal/app/register_test.go` — enrollment token exchanged for a device token stored separately at `0600`; `config.yaml`'s `source.type` becomes `server`; invalid/consumed token leaves it unchanged, exits non-zero.
- [ ] 8.5 GREEN: `internal/app/register.go` + `cmd/aliasdeck/register.go`.
- [ ] 8.6 RED: `internal/app/logout_test.go` — per 1.1: removes the local session only; succeeds even when the server is unreachable.
- [ ] 8.7 GREEN: `internal/app/logout.go` + `cmd/aliasdeck/logout.go`.
- [ ] 8.8 RED: `internal/app/status_test.go` (server case) — reports `ServerSource` + URL; token value never appears in output.
- [ ] 8.9 GREEN: extend `internal/app/status.go`.
- [ ] 8.10 RED: `internal/app/edit_test.go` (server case) — `edit` fails with an explicit error pointing at the API, no file opened; `edit --config` still opens `config.yaml`.
- [ ] 8.11 GREEN: extend `internal/app/edit.go`.
- [ ] 8.12 RED: `internal/app/uninstall_test.go` (server case) — `uninstall` removes the credentials file alongside existing cleanup.
- [ ] 8.13 GREEN: extend `internal/app/uninstall.go`.
- [ ] 8.14 Register `serve`/`login`/`register`/`logout` in `cmd/aliasdeck/root.go` (consolidates 4.5/8.3/8.5/8.7).

## Phase 9: Cross-Cutting Verification

- [ ] 9.1 RED+GREEN: byte-identity integration test — the same aliases through `FileSource` and through serve→sync produce identical rendered bytes and identical `ComputeRevision` (proposal success criterion 2).
- [ ] 9.2 RED+GREEN: full in-process integration test — serve → login → enrollment token → register → sync, `httptest.Server` + `t.Cleanup` shutdown, ephemeral listeners only, no sleep.
- [ ] 9.3 Verify: `internal/archtest/deps_test.go` still passes against the complete implementation.
- [ ] 9.4 Verify: renderer golden files, real bash/zsh injection test, real `pwsh` test are byte-for-byte untouched and still green — **flag immediately if any prior task touched them**; none is expected to.
- [ ] 9.5 Verify: `go test ./...` and `make cover` — new packages ≥70% coverage; `make check` green.

## Phase 10: Release, CI & Docs

- [ ] 10.1 `.goreleaser.yaml` — confirm the six darwin/linux/windows amd64/arm64 targets still build `CGO_ENABLED=0` with the embedded server + `modernc.org/sqlite`.
- [ ] 10.2 `.github/workflows/ci.yml` — wire the 25 MB binary-size gate: measure each built artifact, fail naming the artifact and size if exceeded. **Over-budget contingency**: exclude `serve` via a build tag as an additive follow-up (design decision 1) — not a v0.3 redesign — flagged to the maintainer before release, never silently patched.
- [ ] 10.3 `.github/workflows/ci.yml` — wire `sqlc generate` drift check (2.6) and the OpenAPI route-coverage check (5.13) as CI jobs.
- [ ] 10.4 `docs/PROJECT.md` §9, §10 — correct: `cmd/aliasdeck-server/` not created; root `migrations/` moves to `internal/store/migrations/`; record new deps.
- [ ] 10.5 `docs/API.md` — write the API reference from `docs/openapi.yaml`.
- [ ] 10.6 `README.md` — update the status table for server support.
- [ ] 10.7 `openspec/config.yaml` — note new deps/packages under `context`.
- [ ] 10.8 Release notes — state that migrations are forward-only and a binary downgrade requires restoring the database file backed up before upgrading.
- [ ] 10.9 Final `make check && make cover`; manual smoke: `./aliasdeck serve` in an empty directory starts, migrates, prints the one-time operator credential, serves `/api/v1` with no external service.

## Parallelization

- Phase 1 must land first in full (skeleton + `deps_test.go`); everything else is graded against it.
- Phase 7.1–7.4 (URL guard + credentials file) have no server-side dependency and MAY start immediately after Phase 1, in parallel with Phase 2.
- Phase 3 can start once 2.1's interfaces exist (a fake `Store` suffices for auth's own tests), in parallel with 2.2–2.9's SQLite/migration work.
- Phase 4's skeleton (4.1–4.4) can proceed in parallel with Phase 3; its full wiring (4.6) waits on Phase 5.
- Phase 5 needs Phase 2 (store) and Phase 3 (auth middleware) merged.
- Phase 6 needs Phase 2; can run parallel to Phase 5's non-sync handlers, but 6.3–6.6 need Phase 5's router.
- Phase 7.5–7.9 need Phase 6's response DTO shape.
- Phase 8 needs Phases 3, 6, and 7.
- Phases 9 and 10 are strictly last.
